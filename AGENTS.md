# Operational Rules

1. **Linting:** Before creating a Pull Request or marking a task as complete, you MUST run `pnpm lint:fix` to resolve formatting issues.
2. **Verification:** Ensure that `pnpm lint` passes without errors before submitting.
