enum E_EntityType {
    GALLERY
    BLOG
    MESSAGE
}

union U_Entity = T_Gallery | T_Blog | T_Message

type T_Like {
    id: String
    isDel: Boolean
    createdAt: DateTime
    updatedAt: DateTime
    userId: String
    user: T_User
    entityType: E_EntityType
    entityId: String
    entity: U_Entity
}

input Input_QueryLike {
    id: String
    isDel: Boolean
    createdAt: DateTime
    updatedAt: DateTime
    userId: String
    entityType: E_EntityType
    entityId: String
}

input Input_CreateLike {
    userId: String!
    entityType: E_EntityType!
    entityId: String!
}

type T_Response_Like {
    success: Boolean
    message: String
    result: T_Like
}

type T_Likes {
    docs: [T_Like]
    totalDocs: Int
    limit: Int
    hasPrevPage: Boolean
    hasNextPage: Boolean
    page: Int
    totalPages: Int
    offset: Int
    prevPage: Int
    nextPage: Int
    pagingCounter: Int
    meta: JSON
}

type T_Response_Likes {
    success: Boolean
    message: String
    result: T_Likes
}

type Query {
    getLikes(filter: Input_QueryLike, options: JSON): T_Response_Likes!
}

type Mutation {
    createLike(doc: Input_CreateLike!): T_Response_Like!
    deleteLike(filter: Input_QueryLike!, options: JSON): T_Response_Like!
} 