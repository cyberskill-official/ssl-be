input Input_MakePayment {
    # Accept a free-form JSON payload for flexibility
    doc: JSON!
}

type T_Response_HppOrder {
    success: Boolean!
    message: String
    result: JSON
}

extend type Mutation {
    makePayment(doc: JSON!): T_Response_HppOrder!
}
