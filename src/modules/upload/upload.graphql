enum E_UploadType {
    IMAGE
    VIDEO
    AUDIO
    DOCUMENT
    OTHER
}

enum E_UploadEntity {
    USER
    EVENT
    CONVERSATION
    CATALOGUE
    GALLERY
}

type T_UploadResult {
    url: String!
    moderationMediaId: String!
    status: E_ModerationMediaStatus
    entityId: String
}

type T_Response_Upload {
    success: Boolean!
    message: String
    result: T_UploadResult
}

type Mutation {
    upload(
        type: E_UploadType!
        entity: E_UploadEntity!
        entityId: String
        tagId: String
        file: Upload!
    ): T_Response_Upload!
}
